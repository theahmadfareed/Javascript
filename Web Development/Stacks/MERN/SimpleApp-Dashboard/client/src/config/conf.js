const conf = {
    VITE_APP_URI_API: String(import.meta.env.VITE_APP_URI_API),
}
// there was a name issue with the import.meta.env.VITE_APPWRITE_URL, it was later fixed in debugging video

export default conf