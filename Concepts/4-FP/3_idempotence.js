console.log("Idempotence");

// Idempotence => a function that always returns or does what we expect it to do.
function notGood(){
    return Math.random()
}
console.log(notGood());

// delete a user from the database
// http get-request

// being able to call yourself over and over or inside of yourself but you get the same result
console.log(Math.abs(Math.abs(-5)));

// this gurantes that the code is predictable